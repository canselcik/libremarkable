[package]
name = "rustspy"
version = "0.1.0"
authors = ["not-avail <not-avail@not-avail>"]

[lib]
name = "rustspy"
crate_type = ["dylib"]

[dependencies]
redhook = "0.1.1"
libc = "0.2.37"
librustpad = { path = "../librustpad" }